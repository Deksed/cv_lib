# Copy this file to .env and fill in your values
# .env is gitignored — never commit it

# Dataset root directory (required by cv_lib.data.data_root())
DATA_ROOT=/path/to/dataset

# CVAT annotation export path
CVAT_ANNOTATIONS_PATH=/path/to/cvat/annotations.xml
